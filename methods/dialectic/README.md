# DIALECTIC method profile

Public-safe, interactive method profile for the implemented SPINE Heuristic-Hermeneutic Loop.

## Route

`https://fbratten.github.io/methods/dialectic/`

## Canonical source pin

- repository: `fbratten/spine`
- source pin: `8fa62c1ebfc0c7b65680ef2d89b9953a519e4138`
- core implementation: `spine/patterns/hermeneutic_loop.py`
- supporting grammar: `spine/grammar/ebnf_rv.py`
- working memory: `spine/memory/scratchpad.py`
- dedicated tests: `scripts/tests/test_hermeneutic.py`
- historical Phase 5 tests: `scripts/tests/test_phase5.py`

## Status boundary

Implemented in the inspected SPINE source:

- instrumented and callable loop entry points
- encounter and separate synthesis calls
- scratchpad prior-note loading and per-iteration append
- previous-interpretation and knowledge-unit accumulation
- similarity, explicit-done and max-iteration stopping
- token and duration accounting
- EBNF-RV parsing and formatting
- dedicated mocked behavioral tests
- ten intended temporal markers, including `TIMELESS`

Not established:

- a production caller in the inspected tree
- calibrated epistemic convergence
- automatic model switching
- external academic or user-study validation

## Visual inventory

1. Native Canvas 2D loop anatomy with implemented and conceptual layers.
2. Native Canvas 2D horizon-accumulation graph.
3. Browser-local synthetic scratchpad/horizon assembly lab.
4. Browser-local bounded convergence lab.
5. Browser-local RigVedan knowledge-unit builder.
6. Native Canvas 2D implementation and separation map.
7. Native Canvas 2D repository lineage.
8. Chart.js historical source-reported receipt graph with explicit scope warning.
9. Chart.js current implementation-surface counts.
10. Evidence table, receipt cards, evidence-based hypothesis and non-claims.

## Evidence boundaries

- Browser labs do not call an LLM, execute SPINE or write files.
- Similarity and `is_complete` are model-produced control signals, not truth proofs.
- The four phase enum values do not mean four separately executed or recorded runtime stages.
- Scratchpad prior notes are loaded once before the loop; same-run continuity also depends on explicit in-memory state.
- `ERROR` is a declared reason but was not found as an emitted core-loop outcome.
- Historical test counts use different scopes and were not rerun for publication.
- No clear production caller was identified in the pinned repository search.
- RigVedan annotations classify knowledge state but do not establish truth.

## Public safety

The page contains synthetic subjects, interpretations and knowledge units. It contains no private source text, real scratchpad contents, credentials, local paths, tenant identifiers, operator memory, source conversations or work records. It performs no repository write, source retrieval, LLM call or runtime execution.
