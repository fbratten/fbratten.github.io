# AICS method profile

Public-safe interactive profile for AICS 0.2.0.

## Source pin

- repository: `fbratten/aics`
- commit: `cd88e41bcadcdc68155b384ecf723f3b91a80b18`
- maturity: experimental, self-hosting pilot
- working expansion: Atomic Instruction Communication Standard

## Visual inventory

- native Canvas 2D canonical atom graph
- native Canvas 2D projection topology
- native Canvas 2D conformance dependency graph
- browser-only synthetic execution phase gate
- browser-only deterministic round-trip laboratory
- browser-only bounded changeset laboratory
- native Canvas 2D development lineage
- Chart.js test-growth graph
- Chart.js test-composition graph
- repository receipt cards

## Evidence boundary

The page distinguishes:

- normative specification statements;
- executable reference-tool behaviour visible in source;
- source-reported CI and test receipts;
- counted test functions at the pinned commit;
- synthetic browser demonstrations;
- mechanism hypotheses and future evaluation routes.

The browser laboratories are not the AICS validator, round-trip renderer, changeset executor or CLI.

## Public safety

The profile contains no private contract body, operator memory, credential, local path, tenant identifier or live repository state. It performs no network mutation, LLM call, source retrieval, authorization or execution.

## Visual technology

- Chart.js 4.4.7 on HTML Canvas
- native Canvas 2D for graph models
- pointer interaction, click inspection, filters and layout reset
- HTML inspector panels as text alternatives
- no Mermaid renderer or Mermaid diagram
